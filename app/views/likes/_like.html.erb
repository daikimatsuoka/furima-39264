<% if user_signed_in? && item.liked_by?(current_user) %>
  <%= link_to "★ いいね", item_likes_path(item.id), method: :delete, class: "star-icon",style: "color:red;", remote: true %>
<% else %>
  <%= link_to "★", item_likes_path(item.id), method: :post, class: "star-icon", remote: true %>
<% end %>
